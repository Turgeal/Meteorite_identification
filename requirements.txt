# PyTorch and related
torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.12  # For ConvNeXt, EfficientNet, Swin, MaxViT pretrained models

# Data processing
pandas>=2.0.0
numpy>=1.24.0
Pillow>=10.0.0

# Training utilities
tqdm>=4.65.0
scikit-learn>=1.3.0

# ─── New dependencies for improvement strategies ───
easyocr>=1.7.0       # OCR: detect text like "meteorite" in images
imagehash>=4.3.1     # Duplicate detection: perceptual hash
lightgbm>=4.0.0      # Stacking: meta-learner for feature-level ensemble

# Optional: for better training monitoring
# tensorboard>=2.14.0

# Optional: for faster inference
# onnxruntime-gpu  # for ONNX export if needed
